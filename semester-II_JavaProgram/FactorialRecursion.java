
/*
class FibonacciFactorial {

    // Recursive function for Fibonacci
    static int fibonacciRecursive(int n) {
        if (n <= 1)
            return n;
        return fibonacciRecursive(n - 1) + fibonacciRecursive(n - 2);
    }

    // Non-recursive (iterative) function for Fibonacci
    static void fibonacciNonRecursive(int n) {
        int a = 0, b = 1, c;
        System.out.print("Fibonacci Series: " + a + " " + b);

        for (int i = 2; i < n; i++) {
            c = a + b;
            System.out.print(" " + c);
            a = b;
            b = c;
        }
        System.out.println();
    }

    // Recursive function for Factorial
    static int factorialRecursive(int n) {
        if (n == 0)
            return 1;
        return n * factorialRecursive(n - 1);
    }

    // Non-recursive (iterative) function for Factorial
    static int factorialNonRecursive(int n) {
        int fact = 1;
        for (int i = 1; i <= n; i++) {
            fact = fact * i;
        }
        return fact;
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        System.out.print("Enter a number: ");
        int n = sc.nextInt();

        // Fibonacci
        System.out.println("\nFibonacci using Recursion:");
        for (int i = 0; i < n; i++) {
            System.out.print(fibonacciRecursive(i) + " ");
        }

        System.out.println("\n\nFibonacci using Non-Recursion:");
        fibonacciNonRecursive(n);

        // Factorial
        System.out.println("\nFactorial using Recursion: " + factorialRecursive(n));
        System.out.println("Factorial using Non-Recursion: " + factorialNonRecursive(n));

        sc.close();
    }
}  */

    /* Fibonacci series */
    import java.util.Scanner;
    
        public static void main(String[] args){
           
                       }
    }
    